apiVersion: apps/v1
kind: StatefulSet
metadata:
  # This name uniquely identifies the stateful set
  name: yolov8-stateful
  labels:
    app: yolov8
spec:
  serviceName: yolov8
  replicas: 1
  selector:
    matchLabels:
      app: yolov8
  template:
    metadata:
      labels:
        app: yolov8
    spec:
      containers:
        - name: yolov8
          image: ghcr.io/swiss-ai-center/yolov8:latest
          # If you build the image locally, change the next line to `imagePullPolicy: Never` - there is no need to pull the image
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
          env:
            - name: MAX_TASKS
              value: "50"
            - name: ENGINE_ANNOUNCE_RETRIES
              value: "5"
            - name: ENGINE_ANNOUNCE_RETRY_DELAY
              value: "3"
          envFrom:
            - configMapRef:
                name: yolov8-config
